#include <stdio.h>

/* this is a stub funtion, A stub is a function that has the expected signature
 * (i.e. name and accepted arguments), but an incomplete implementation. */
int get_temperature(int outside)
{
	return 24;
}

int main(void)
{

	int t = get_temperature(0);
	return t;
}
