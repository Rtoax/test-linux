#ifndef S_MODE
# error "Not define S_MODE(m)"
#endif
S_MODE(S_IFMT);
S_MODE(S_IFDIR);
S_MODE(S_IFCHR);
S_MODE(S_IFBLK);
S_MODE(S_IFREG);
S_MODE(S_IFIFO);
S_MODE(S_IFLNK);
S_MODE(S_IFSOCK);
S_MODE(S_ISUID);
S_MODE(S_ISGID);
S_MODE(S_ISVTX);
S_MODE(S_IRUSR);
S_MODE(S_IWUSR);
S_MODE(S_IXUSR);
S_MODE(S_IRWXU);
S_MODE(S_IRGRP);
S_MODE(S_IWGRP);
S_MODE(S_IXGRP);
S_MODE(S_IRWXG);
S_MODE(S_IROTH);
S_MODE(S_IWOTH);
S_MODE(S_IXOTH);
S_MODE(S_IRWXO);
