#!/bin/env bpftrace
macro bpf_arithmetic_sum() {
  import "loop.bpf.c";

  $cnt = __bpf_arithmetic_sum(4);
  $cnt
}

BEGIN
{
  $cnt1 = bpf_arithmetic_sum();
  printf("count %d\n", $cnt1);
  exit();
}
