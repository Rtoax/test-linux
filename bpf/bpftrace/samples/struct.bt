#!/bin/env bpftrace
// Could have whitespace
	struct a0 {
	int a;
};

// TODO: test __attribute__
struct __attribute__((packed)) a1 {
	int i;
	char c;
};

/*
struct a2 {
	int i;
	char c;
} __attribute__((packed));
*/

union u0 {
	int a;
	int b;
};

enum e0 {
	a,
	b,
};

BEGIN
{
	$a = (struct a0 *)0;
	exit();
}
