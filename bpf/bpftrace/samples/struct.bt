#!/bin/env bpftrace
// Could have whitespace
	struct a0 {
	int a;
};

// TODO: test __attribute__
struct __attribute__((packed)) a1 {
	int i;
	char c;
	struct a0 a0;
	long l;
};

/*
struct a2 {
	int i;
	char c;
} __attribute__((packed));
*/

union u0 {
	int a;
	int b;
};

enum e0 {
	a,
	b,
};

BEGIN
{
	$a = (struct a0 *)0;
	printf("offsetof %d\n", offsetof(struct a1, l));
	exit();
}
