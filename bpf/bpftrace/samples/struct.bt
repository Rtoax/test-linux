#!/bin/env bpftrace
// Could have whitespace
	struct a0 {
	int a;
};

/* TODO: test __attribute__
struct a1 __attribute__((packed)) {
};
*/

union u0 {
	int a;
};

enum e0 {
	a,
	b,
};

BEGIN
{
	$a = (struct a0 *)0;
	exit();
}
