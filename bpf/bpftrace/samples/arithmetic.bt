#!/bin/env bpftrace

BEGIN
{
	$addr = 0x155148;
	$bias = 0x564f61f77000;

	printf("addr %lx\n", $addr);
	printf("bias %lx\n", $bias);
	printf("addr + bias %lx\n", $addr + $bias);

	$addr += $bias;
	printf("addr %lx\n", $addr);

	exit();
}
