Overlay Network
===============

- `Overlay`网络和`Underlay`网络是一组相对概念.
- 为了摆脱`Underlay`网络的种种限制，现在多采用网络虚拟化技术在`Underlay`网络之上创建虚拟的`Overlay`网络.
- 在`Overlay`网络中，设备之间可以通过逻辑链路，按照需求完成互联形成`Overlay`拓扑.
- 相互连接的`Overlay`设备之间建立隧道，数据包准备传输出去时，设备为数据包添加新的IP头部和隧道头部，并且被屏蔽掉内层的IP头部，数据包根据新的IP头部进行转发.

