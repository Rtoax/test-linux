#!/bin/env bpftrace

BEGIN
{
	printf("Tracing libc memcpy. Hit ctrl-c to end\n");
}

uprobe:libc:__memcpy_*,
uprobe:libc:__wmemcpy*,
uprobe:libc:memcpy*
{
	@copy[probe, comm] = count();
}

i:s:1
{
	printf("\n");
	print(@copy, 10);
}
