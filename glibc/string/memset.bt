#!/bin/env bpftrace

BEGIN
{
	printf("Tracing libc memset. Hit ctrl-c to end\n");
}

uprobe:libc:__memset_*,
uprobe:libc:__wmemset*,
uprobe:libc:memset*
{
	@set[probe, comm] = count();
}

i:s:1
{
	printf("\n");
	print(@set, 10);
}
