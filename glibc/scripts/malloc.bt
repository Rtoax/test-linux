#!/bin/env bpftrace
/**
 * File: malloc.bt
 * Author: Rong Tao <rtoax@foxmail.com>
 * Date: 2022.06.08
 *
 */
BEGIN
{
	printf("Tracing malloc(), hit ctrl-c to end.\n");
}

uprobe:libc:malloc
{
	$size = arg0;
	@malloc = hist($size);

	/* Print 'ls' malloc() */
	if (comm == "ls") {
		printf("ls malloc %d\n", $size);
	}
}

