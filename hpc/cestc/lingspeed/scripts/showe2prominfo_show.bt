#!/bin/env bpftrace

BEGIN
{
	printf("Tracing lingspeed sched, hit ctrl-c to end.\n");
	printf("%-8s %-8s %-16s\n", "TIME", "PID", "COMM");
}

kprobe:showe2prominfo_show
{
	@buf[tid] = arg2;
}

kretprobe:showe2prominfo_show
/ @buf[tid] /
{
	printf("buf = %s\n", str(@buf[tid]));
	delete(@buf[tid]);
}

END
{
	clear(@buf);
	printf("Bye.\n");
}
