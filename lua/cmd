#!/bin/env lua

-- Execute command
cmd = function(cmd)
	cmd = io.popen(cmd, 'r')
	cmd = cmd:read('a+')
	return cmd
end

erg = cmd('cat /etc/os-release')
print(erg)

erg = cmd('test -e /lib64/libc.so.6 && echo /lib64/libc.so.6')
print(erg)
